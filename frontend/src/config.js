// API Configuration
const isProduction = process.env.NODE_ENV === 'production';

// API Configuration (Only Render)
export const API_URL = 'https://companies-directory-39z3.onrender.com';

export default { API_URL };
